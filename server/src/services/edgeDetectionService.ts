import { Node, Edge } from "database";

/**
 *  detect nodes related to the given node and create edges attaching them to it
 */
export const detectEdgesForNode = async (node: Node): Promise<Edge[]> => {
  return [];
};
